<Window x:Class="PerfMonCS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PerfMonCS"
        Title="PerfMon"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Left="200" Top="200"
        Width="90" Height="240"
        MinWidth="50" MinHeight="40"
        FontFamily="Segoe UI"
        MouseLeftButtonDown="OnDrag"
        LocationChanged="OnStateChanged"
        SizeChanged="OnStateChanged">

    <Border x:Name="RootBorder"
            Background="#D9080A10"
            BorderBrush="#12FFFFFF"
            BorderThickness="1"
            CornerRadius="5">
        <!-- 글자 스케일(Viewbox) 토글 시 MainGrid 를 이 호스트와 Viewbox 사이로 재배치 -->
        <Grid x:Name="HostGrid">
            <Grid x:Name="MainGrid">

                <Rectangle x:Name="Sep1" Fill="#0CFFFFFF" Visibility="Collapsed"/>
                <Rectangle x:Name="Sep2" Fill="#0CFFFFFF" Visibility="Collapsed"/>
                <Rectangle x:Name="Sep3" Fill="#0CFFFFFF" Visibility="Collapsed"/>
                <Rectangle x:Name="Sep4" Fill="#0CFFFFFF" Visibility="Collapsed"/>

                <!-- 색상은 런타임에 ApplyColors()가 섹션 설정(기본값 포함)으로 채운다 -->

                <!-- CPU -->
                <Grid Margin="4,3,4,2" x:Name="PanelCpu">
                    <local:GraphControl x:Name="gCpu"/>
                    <Grid x:Name="hdrCpu" Panel.ZIndex="1" VerticalAlignment="Top" Margin="0,0,0,2">
                        <TextBlock x:Name="lblCpu" Text="CPU" FontSize="10" FontWeight="SemiBold"/>
                        <StackPanel x:Name="valsCpu" HorizontalAlignment="Right" Orientation="Horizontal">
                            <TextBlock x:Name="vCpu"  Text="0%"  FontSize="10" FontWeight="Bold"/>
                            <TextBlock x:Name="vTemp" Text=""    FontSize="10" Opacity="0.65" Margin="3,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Grid>

                <!-- MEM -->
                <Grid Margin="4,3,4,2" x:Name="PanelMem">
                    <local:GraphControl x:Name="gMem"/>
                    <Grid x:Name="hdrMem" Panel.ZIndex="1" VerticalAlignment="Top" Margin="0,0,0,2">
                        <TextBlock x:Name="lblMem" Text="MEM" FontSize="10" FontWeight="SemiBold"/>
                        <StackPanel x:Name="valsMem" HorizontalAlignment="Right" Orientation="Horizontal">
                            <TextBlock x:Name="vMem"   Text="0%"   FontSize="10" FontWeight="Bold"/>
                            <TextBlock x:Name="vMemGB" Text=""     FontSize="10" Opacity="0.65" Margin="3,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Grid>

                <!-- DISK: R=Value1/Graph1, W=Value2/Graph2 -->
                <Grid Margin="4,3,4,1" x:Name="PanelDisk">
                    <local:GraphControl x:Name="gDisk" IsDual="True"/>
                    <StackPanel x:Name="hdrDisk" Panel.ZIndex="1" VerticalAlignment="Top">
                        <TextBlock x:Name="lblDisk" Text="DISK" FontSize="10" FontWeight="SemiBold"/>
                        <StackPanel x:Name="valsDisk" Margin="0,2,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="R:" FontSize="10" FontWeight="SemiBold"/>
                                <TextBlock x:Name="vDR" Text="0" FontSize="10" FontWeight="Bold" Margin="1,0,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="W:" FontSize="10" FontWeight="SemiBold"/>
                                <TextBlock x:Name="vDW" Text="0" FontSize="10" Margin="1,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Grid>

                <!-- NET: D=Value1/Graph1, U=Value2/Graph2 -->
                <Grid Margin="4,3,4,2" x:Name="PanelNet">
                    <local:GraphControl x:Name="gNet" IsDual="True"/>
                    <StackPanel x:Name="hdrNet" Panel.ZIndex="1" VerticalAlignment="Top">
                        <TextBlock x:Name="lblNet" Text="NET" FontSize="10" FontWeight="SemiBold"/>
                        <StackPanel x:Name="valsNet" Margin="0,2,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="D:" FontSize="10" FontWeight="SemiBold"/>
                                <TextBlock x:Name="vNDn" Text="0" FontSize="10" FontWeight="Bold" Margin="1,0,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="U:" FontSize="10" FontWeight="SemiBold"/>
                                <TextBlock x:Name="vNUp" Text="0" FontSize="10" Margin="1,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
