# harness-assets.txt — ai-agents-config 저장소가 각 프로젝트에 배포하는 하네스 운영 자산의 유일한 목록(SSOT).
#
# sync-configs.ps1 -Action Push 는 이 목록의 자산만 각 프로젝트 scripts\ 로 복사한다.
# scripts/verify.ps1 과 task-status.ps1 는 같은 목록을 읽어 드리프트를 판정한다.
#
# 주의:
#  - 한 줄에 자산 파일명 하나만 쓴다. 빈 줄과 '#' 으로 시작하는 주석 줄은 무시한다.
#  - 경로가 아니라 파일명만 쓴다 (디렉터리 트래버셜 금지 — global/harness 바로 아래만).
#  - 중복 항목은 제거된다. 목록 순서는 판정에 영향을 주지 않는다.
#
# 이 파일 자신도 배포 대상이다 (마지막 항목). 중앙 전용 문서(orchestration-runbook.md,
# promotion-notes.md)는 프로젝트에 배포하지 않으므로 이 목록에 넣지 않는다.
dispatch-with-hang-detect.ps1
check-packet-archive.ps1
model-profiles.json
model-profile.ps1
orchestrate-packet.ps1
session-health.ps1
gy-orchestrator-prompt.md
commit-harness-sync.ps1
task-status.ps1
sweep-orphan-browsers.ps1
stage-thresholds.json
harness-contracts.ps1
harness-io.ps1
harness-assets.txt
